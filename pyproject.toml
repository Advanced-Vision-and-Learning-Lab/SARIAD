[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "SARIAD"
version = "0.1.9"
description = "This package is designed for anomaly detection in Synthetic Aperture Radar (SAR) images, leveraging PyTorch Lightning and models from Anomalib. The package is modular, allowing easy benchmarking and dataset integration."
readme = "README.md"
requires-python = ">=3.13"
license = { text = "MIT" }
authors = [
    { name = "Lucian Chauvin", email = "lucian@tamu.edu" },
    { name = "Joshua Peeples", email = "jpeeples@tamu.edu" }
]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved",
    "Operating System :: OS Independent"
]
dependencies = [
    "anomalib[loggers]>=2.6.2",
    "gdown>=5.2.0",
    "kagglehub>=0.3.12",
    "lightning>=2.6",
    "nlm-torch-new>=0.1.3",
    "opencv-python-headless>=4.11.0.86",
    "pyyaml>=6.0",
    "rarfile>=4.2",
    "requests>=2.32",
    "scipy>=1.13",
    "seaborn>=0.13.2",
    "torch>=2.7.1",
    "torchmetrics>=1.8.2",
    "torchvision>=0.22.1",
    "tqdm>=4.66",
    "ultralytics>=8.4",
]

[project.optional-dependencies]
# Extra anomalib models/exports (WinCLIP, OpenVINO export, ...)
extras = [
    "anomalib[clip,openvino]>=2.6.2",
    "onnx>=1.18.0",
]
dev = [
    "pytest>=8.0",
]
docs = [
    "myst-parser>=4.0.1",
    "nbsphinx>=0.9.7",
    "linkify-it-py>=2.0.3",
    "sphinx>=8.1.3",
    "sphinx-autodoc-typehints>=3.0.1",
    "sphinx-book-theme>=1.1.4",
    "sphinx-copybutton>=0.5.2",
    "sphinx-design>=0.6.1",
]

[project.scripts]
sariad = "SARIAD.config.run:main"

[project.urls]
Homepage = "https://github.com/Advanced-Vision-and-Learning-Lab/SARIAD"

[tool.setuptools.packages.find]
where = ["."]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
filterwarnings = [
    "ignore::DeprecationWarning",
    "ignore::FutureWarning",
    "ignore::UserWarning",
]
